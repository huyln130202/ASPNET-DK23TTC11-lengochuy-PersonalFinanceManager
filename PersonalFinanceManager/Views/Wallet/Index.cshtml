@model IEnumerable<PersonalFinanceManager.Models.Wallet>

@{
    ViewData["Title"] = "Ví";
}

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1>Danh Sách Ví</h1>
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Thêm Ví Mới
        </a>
    </div>

    <div class="row">
        @foreach (var wallet in Model)
        {
            <div class="col-md-4 mb-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">@wallet.Name</h5>
                        <p class="card-text">
                            <strong>Loại:</strong> @wallet.Type<br>
                            <strong>Số Dư:</strong> @wallet.Balance.ToString("#,##0")<br>
                            @if (!string.IsNullOrEmpty(wallet.Description))
                            {
                                <strong>Mô Tả:</strong> @wallet.Description
                            }
                        </p>
                        <div class="btn-group">
                            <a asp-action="Edit" asp-route-id="@wallet.Id" class="btn btn-outline-primary">
                                <i class="bi bi-pencil"></i> Sửa
                            </a>
                            <form asp-action="Delete" asp-route-id="@wallet.Id" method="post" class="d-inline">
                                <button type="submit" class="btn btn-outline-danger" 
                                        onclick="return confirm('Bạn có chắc chắn muốn xóa ví này?')">
                                    <i class="bi bi-trash"></i> Xóa
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div> 